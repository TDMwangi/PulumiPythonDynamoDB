name: PulumiPythonDynamoDB
runtime:
  name: python
  options:
    toolchain: pip
    virtualenv: venv
description: Pulumi AWS DynamoDB deployment
config:
  pulumi:tags:
    value:
      pulumi:template: aws-python
